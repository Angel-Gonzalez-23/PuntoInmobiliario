header(class="bg-indigo-600 p-0.5") 
    .container.mx-auto.flex.justify-between.items-center
        a(href="/") 
            h1.text-2xl.text-white.font-extrabold.text-center.ml-2  Bienes
                span.font-normal Raices 
        nav(class="my-5 mx-5 text-sm md:flex md:items-center md:gap-3 font-bold text-white hidden")
            a(href="mis-propiedades") Mis propiedades
            a(href="" class=" text-white font-bold uppercase cursor-pointer") Mi Perfil

            form(action="/auth/cerrar-sesion" method="POST") 
                input(type="hidden" name="_csrf" value=csrfToken)
                input(type="submit" class="bg-indigo-900 py-2 px-10 rounded-lg" value="Cerrar Sesión") 

//- modificar heade cno mediaquery si es necesario (el margin top y bottom)
